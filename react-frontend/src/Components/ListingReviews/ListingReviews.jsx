import PropTypes from "prop-types";
import "./ListingReviews.scss";

const ListingReviews = ({listing}) => {

    return (
        <div className="reviewSection">

        </div>
    )
}

ListingReviews.propTypes = {
    listing: PropTypes.shape({

    }),
};

export default ListingReviews;